/*excpectThat = require('expectThat.mocha')

{apply,
copy,
unary,
binary,
flip2,
is-string,
is-array,
is-number,
is-function,
is-args,
is-regex,
is-seq,
isnt-empty,
all-with,
map-with,
at-with,
filter-with,
mapcat-with,
split-at-with,
all-strings,
all-arrays,
chars,
unchars,
index-of,
indices-of,
list-equals,
starts-with,
is-sub-seq,
strContains,
to-number,
drop-last} = require '../lib/base'


describe 'base helpers', ->
  args-len = -> arguments.length
  add = (a,b)-> a + b
  arr = [1,2,3]
  three-args = (a,b,c,d)-> arguments.length

#  copy
  expectThat -> (arr |> copy).shouldnt be arr
  expectThat -> (arr |> copy |> (.length)).should be arr.length

#  unary
  expectThat -> (add |> unary |> (.length)).should be 1
  expectThat -> (unary(args-len)(1,2,3,4)).should be 1
  expectThat -> (unary(is-string)('')).should be true

  #  binary
  expectThat -> (binary(add).length).should be 2
  expectThat -> (binary(args-len)(1,2,3,4)).should be 2
  expectThat -> (binary(add)(1,2)).should be 3*/
  #  is-seq

  #  isnt-empty

  #  all-with

  #  map-with

  #  at-with

  #  filter-with

  #  mapcat-with

  #  split-at-with

  #  all-strings

  #  all-arrays

  #  chars

  #  unchars

  #  index-of

  #  indices-of

  #  list-equals

  #  starts-with

  #  is-sub-seq

  #  strContains

  #  to-number

  #  drop-last
